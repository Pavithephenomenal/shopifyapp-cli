import React from "react";
import axios from "axios";

const Id = (props) => {
    return ( 
    <page >
        < div > Hello world: { props.name } </div>  
        </page>
    );
};
export default Id;